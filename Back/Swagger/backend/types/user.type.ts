type User = {
    uid : number,
    userName :  string,
    userId : string,
    userPw : string
};

type RequestUser = {
    uid : number,
    userName :  string,
    userId : string,
    userPw : string
};

export  {User, RequestUser};