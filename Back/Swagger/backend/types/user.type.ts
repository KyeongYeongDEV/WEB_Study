type User = {
    uid : number,
    name :  string,
    userId : string,
    userPw : string
};

type RequestUser = {
    uid : number,
    name :  string,
    userId : string,
    userPw : string
};

export  {User, RequestUser};